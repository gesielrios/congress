<%= simple_form_for @clean, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                clean_path, :class => 'btn' %>
  </div>
<% end %>
